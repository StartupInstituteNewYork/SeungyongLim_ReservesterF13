
	<h2>Reservation:</h2>
	<%= form_for([@restaurant,@restaurant.reservations.build]) do |f| %>
	 <p>
	 	<%= f.label :email %>
	 	<%= f.text_field :email %>
	 </p>
	 <p>
	<span class="center"><%= f.file_field :menu %></span>
	 </p>
	 <p>
	 	<%= f.label :message %>  
	 	<%= f.text_area :message, :cols=>30, :rows=> 20 %>  
	 </P> 	
	 <p>
	 	<%= f.submit "Reservation Msg", class: "btn btn-lg btn-primary" %>
	 </p>	
		<% end %> 
	